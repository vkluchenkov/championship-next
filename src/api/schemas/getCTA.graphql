query getCTA {
  page(id: "settings-championship", idType: URI) {
    title
    cta {
      en {
        header
        text
        buttonEnabled
        buttonLink
        buttonText
      }
      ru {
        header
        text
        buttonEnabled
        buttonLink
        buttonText
      }
    }
  }
}
